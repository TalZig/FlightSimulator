<Window x:Class="FlightSimulator.SubMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlightSimulator"
        xmlns:View="clr-namespace:FlightSimulator.View"
        mc:Ignorable="d"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        ResizeMode="NoResize"
        Title="FlightGearController" Height="600" Width="1000">
    <Grid Margin="0,-2,0.333,1.667" Background="BlueViolet">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="81*"/>
            <ColumnDefinition Width="913*"/>
        </Grid.ColumnDefinitions>
        <View:ControlBoard x:Name="Board" Margin="514,18,97.667,300.333" RenderTransformOrigin="0.5,0.5" Grid.Column="1">
            <View:ControlBoard.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleX="0.7" ScaleY="0.7"/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </View:ControlBoard.RenderTransform>
        </View:ControlBoard>

        <View:Map x:Name="map" Margin="20,50,552.667,37.333" RenderTransformOrigin="0.5,0.5" Loaded="Map_Loaded" Grid.ColumnSpan="2"></View:Map>
        <Button Content="stop fly" HorizontalAlignment="Left" Margin="34,27,0,0" VerticalAlignment="Top" Width="88" Click="Button_Click" Grid.ColumnSpan="2" />
        <TextBlock HorizontalAlignment="Left" Background="OrangeRed" Margin="369,105,0,0" TextWrapping="Wrap" Text="{Binding Path= VMStatusOfServer, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" VerticalAlignment="Top" Height="41" Width="86" Grid.Column="1"/>
        <TextBlock x:Name="Boundaries" HorizontalAlignment="Left" Background="Gray" Margin="369,189,0,0" TextWrapping="Wrap" Text="{Binding Path= VMMsg, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" VerticalAlignment="Top" Height="45" Width="88" Grid.Column="1"/>
        <View:MyJoystick x:Name="myJoystick" HorizontalAlignment="Left" Height="300" Margin="449,246,0,0" VerticalAlignment="Top" Width="330" Grid.Column="1"/>
        <Label Content="Plane's Status:" HorizontalAlignment="Left" Margin="366,163,0,0" VerticalAlignment="Top" Width="90" Height="28" Grid.Column="1"/>
        <Label Content="Server's Status:" HorizontalAlignment="Left" Margin="365,80,0,0" VerticalAlignment="Top" Grid.Column="1"/>
    </Grid>

</Window>
